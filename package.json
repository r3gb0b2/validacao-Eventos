
{
  "name": "st-checkin-ios",
  "version": "1.0.0",
  "description": "App de Check-in ST Ingressos para iPhone",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Simulando build...' && mkdir -p dist && cp index.html dist/ && cp -r components dist/ && cp -r hooks dist/ && cp -r utils dist/ && cp *.tsx dist/ && cp *.ts dist/ && cp manifest.json dist/",
    "cap-add-ios": "npx @capacitor/cli add ios",
    "cap-sync": "npx @capacitor/cli sync",
    "cap-open": "npx @capacitor/cli open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "firebase": "^10.12.2",
    "html5-qrcode": "^2.3.8",
    "jspdf": "^2.5.1",
    "papaparse": "^5.4.1",
    "jszip": "^3.10.1",
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
